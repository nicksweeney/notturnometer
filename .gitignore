# Project data — scraped/derived, not source
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.csv
ttn_summary_cache.json
ttn_projection_cache.json
ttn_slug_cache.json
ttn_top20_works_timeline_5y.svg
site.sqlite
site.sqlite.tmp
dist/

# Python / uv
.venv/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / Pagefind byproduct — `npx --yes pagefind` stubs an empty package.json
# + package-lock.json in the project root on every render (npm won't install
# without one); NOT real deps (pagefind is fetched ad hoc via npx --yes).
package.json
package-lock.json
node_modules/

# Editor / OS
.DS_Store
.vscode/
.idea/
.claude/
*.swp
*.swo
*~

# Scratch space (handy to have during debugging,
# but you don't want it on the public record)
/tmp/
/scratch/

# Internal dev artifacts — kept locally, not part of the public repo
CLAUDE.md
docs/superpowers/
README-orig.md
README-new.md

# Cerys consultation transcript (local log)
cerys.txt

# Ad-hoc corpus analysis notes (local log)
analysis_notes.txt
